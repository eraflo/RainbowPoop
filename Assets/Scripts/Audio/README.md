# Audio

## AudioManager

Responsible to play each sound.

2 functions :
- `createAudioAtLocation` : play an audio at a specific location
- `createAudio` : just play an audio

Contain the __audio effect settings__. You need to have 1 `AudioEffectSettings` for each `AudioEffectType`.

